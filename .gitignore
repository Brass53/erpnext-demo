# Bench / ERPNext-Artefakte NICHT einchecken
frappe-bench/
sites/*/site_config.json
common_site_config.json

# Build / Dependencies
node_modules/
__pycache__/
*.pyc
env/
.venv/

# OS / Editor
.DS_Store
.idea/
*.log

# WICHTIG: Das Demo-Backup unter backup/ wird BEWUSST eingecheckt,
# damit der Prof die Site 1:1 wiederherstellen kann (nur ~1 MB).
# Erst wenn ein Backup >50 MB wird, auf GitHub-Release/LFS auslagern:
# backup/*.tar
# backup/*.sql.gz
